{
  "name": "uat",
  "version": "1.0.0",
  "main": "cypress.config.js",
  "scripts": {
    "start": "serve build -l 3000",
    "build": "rimraf build && mkdir build && cp -R \"Homepage Folder\" \"SignIn Folder\" \"SignUp Folder\" index.html public build",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test:ci": "npm run build && serve build -l 3000 & npx wait-on http://localhost:3000 && cypress run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cypress/code-coverage": "^3.14.0",
    "babel-plugin-istanbul": "^7.0.0",
    "cypress": "^14.3.0",
    "istanbul-lib-coverage": "^3.2.2",
    "istanbul-lib-processinfo": "^2.0.3",
    "nyc": "^17.1.0",
    "rimraf": "^6.0.1",
    "serve": "^14.2.4",
    "wait-on": "^8.0.3"
  },
  "overrides": {
    "rimraf": "^6.0.1"
  }
}
